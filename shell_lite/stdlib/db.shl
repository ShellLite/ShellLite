to db_open path
    give std_db_open(path)

to db_close
    std_db_close()

to db_exec sql, params=[]
    give std_db_exec(sql, params)

to db_query sql, params=[]
    give std_db_query_rows(sql, params)
